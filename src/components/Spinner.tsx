import { MrMiyagi } from "@uiball/loaders";

export default function Spinner() {
  return (
    <div className={"flex items-center justify-center max-w-20 h-full"}>
      <MrMiyagi size={120} color={"#22c55e"} />
    </div>
  );
}
